<html>
<head>
<title>SDK Notes: @PACKAGE@-@VERSION@</title>
</head>
<body>
<h1 align=center>SDK Notes: @PACKAGE@-@VERSION@</h1>
<hr>
<h2>Feature Notes:</h2>
<ul>
<li>@PACKAGE@ API HTML Documentation:
  <a href="@PACKAGE@/index.html">off-line</a> (if installed), or
  <a href="http://coin3d.github.io/@PACKAGE@/">on-line</a>
</ul>
<hr>
<h2>Two @PACKAGE@ Libraries?</h2>
If you install the Binary SDK, you will find that it includes
two builds of the @PACKAGE@ library:
<ol>
<li>shared release (@PACKAGE@@SIMVOLEON_MAJOR_VERSION@.lib w/@PACKAGE@@SIMVOLEON_MAJOR_VERSION@.dll)<br>
    (default configuration)
<li>shared debug (@PACKAGE@@SIMVOLEON_MAJOR_VERSION@d.lib w/@PACKAGE@@SIMVOLEON_MAJOR_VERSION@d.dll)<br>
    (configuration name: <strong>debug</strong>)
</ol>
<p>
@PACKAGE@@SIMVOLEON_MAJOR_VERSION@.lib has been linked with the multithread dynamic
C library (msvcrt.dll - the /MD compiler option) and was compiled without
debug tests and symbol information.
<p>
@PACKAGE@@SIMVOLEON_MAJOR_VERSION@d.lib has been linked with the multithread dynamic
debug C library (msvcrtd.dll) and was compiled with debug tests and symbol
information.
<p>
When using Cygwin (or another Bourne shell for MS Windows),
you can run the <tt><strong>@PACKAGE@-config</strong></tt> script
with the option <tt>--alternate=&lt;configuration-name&gt;</tt>
to decide which @PACKAGE@ configuration to retrieve information for.
<p>

<hr>
</body>
</html>
